## Playbooks

- `site.yml`: composición principal basada en roles y topología normalizada.
- `aio.yml`, `cluster.yml`: entrypoints por escenario.
- `backup.yml`, `restore.yml`, `recycle.yml`: wrappers operativos para acciones puntuales.
