- name: Restart dialer pods
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  loop: "{{ dialer_changed_services | default([]) }}"
  when: (dialer_changed_services | default([]) | length) > 0
  tags:
    - install
    - upgrade
    - update
