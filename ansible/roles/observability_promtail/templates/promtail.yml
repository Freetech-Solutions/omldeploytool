server:
  http_listen_port: 9080
  http_listen_address: {{ '0.0.0.0' }}

positions:
  filename: /tmp/positions.yaml 
  sync_period: 1m

clients:
{% if tenant_id is defined and tenant_id != '' %}
  - url: http://{{ loki_host | default('127.0.0.1') }}:3100/loki/api/v1/push
    tenant_id: {{ tenant_id }}
{% else %}
  - url: http://{{ loki_host | default('127.0.0.1') }}:3100/loki/api/v1/push
{% endif %}
    batchsize: 1048576
    batchwait: 1s
    backoff_config:
      min_period: 1s
      max_period: 10s

# ============================================
# DIALER SERVICE MACROS (Defined first)
# ============================================
{%- macro dialer_worker_type_config(worker_type, node_type) -%}
- job_name: dialer_process_{{ worker_type }}
  journal:
    json: false
    max_age: 6h
    path: /var/log/journal
    labels:
      job: dialer_process_{{ worker_type }}
      tenant: {{ tenant_id | default('unknown') }}
      host: {{ inventory_hostname }}
      node_type: {{ node_type }}
      service_family: dialer
      service_type: worker
      worker_type: {{ worker_type }}
      total_replicas: {{ vars['dialer_process_' ~ worker_type ~ '_replicas'] | default('unknown') }}
  relabel_configs:
    - source_labels: ['__journal__systemd_unit']
      regex: 'dialer_process_{{ worker_type }}@.*\.service'
      action: keep
    - source_labels: ['__journal__systemd_unit']
      target_label: 'unit'
    - source_labels: ['__journal__systemd_unit']
      regex: 'dialer_process_{{ worker_type }}@(.+)\.service'
      target_label: 'instance_id'
      replacement: '${1}'
    - source_labels: ['instance_id']
      target_label: 'worker_instance'
      replacement: '{{ worker_type }}-${1}'
    - source_labels: ['__journal_container_name']
      target_label: 'container_name'
    - source_labels: ['__journal_container_id']
      regex: '(.{12}).*'
      target_label: 'container_id'
      replacement: '${1}'
{%- endmacro -%}

{%- macro dialer_services_config(node_type, services_list) -%}
{% for service in services_list %}
- job_name: {{ service }}
  journal:
    json: false
    max_age: 6h
    path: /var/log/journal
    matches: "_SYSTEMD_UNIT={{ service }}.service"
    labels:
      job: {{ service }}
      tenant: {{ tenant_id | default('unknown') }}
      host: {{ inventory_hostname }}
      node_type: {{ node_type }}
      service_family: dialer
  relabel_configs:
    - source_labels: ['__journal__systemd_unit']
      target_label: 'unit'
    - source_labels: ['__journal_container_name']
      target_label: 'container_name'
    - source_labels: ['__journal_container_id']
      regex: '(.{12}).*'
      target_label: 'container_id'
      replacement: '${1}'
{% endfor %}
{%- endmacro -%}

{%- macro dialer_all_services_block() -%}
{% if inventory_hostname in groups.get('omnileads_aio', []) %}
{% set node_type = 'aio' %}
{% elif inventory_hostname in groups.get('dialer_workers', []) %}
{% set node_type = 'dialer' %}
{% else %}
{% set node_type = 'standalone' %}
{% endif %}
{{ dialer_worker_type_config('campaign', node_type) | indent(2, true) }}
{{ dialer_worker_type_config('contact', node_type) | indent(2, true) }}
{{ dialer_worker_type_config('event', node_type) | indent(2, true) }}
{% set all_dialer_services = [
  'dialer_incidence_rules', 'dialer_manage_campaign', 'dialer_flask',
  'dialer_postgresql', 'dialer_render_template', 'dialer_send_reports',] %}
{{ dialer_services_config(node_type, all_dialer_services) | indent(2, true) }}
{%- endmacro -%}

# ============================================
# SCRAPE CONFIGURATIONS (grupos pod del inventario; varios bloques pueden aplicar al mismo host)
# ============================================
scrape_configs:
{% if inventory_hostname in groups.get('omnileads_aio', []) %}
# ============================================
# omnileads_aio: stack completo en un host
# ============================================
{% set promtail_compute_node_type = 'aio' %}
{% set aio_services_full = [
  {'name': 'acd-app', 'unit': 'acd-app.container', 'family': 'acd'},
  {'name': 'acd-conf', 'unit': 'acd-conf.container', 'family': 'acd'},
  {'name': 'acd-server', 'unit': 'acd-server.container', 'family': 'acd'},
  {'name': 'acd-fastagi', 'unit': 'acd-fastagi.container', 'family': 'acd'},
  {'name': 'background_callrec_tasks', 'unit': 'background_callrec_tasks.service', 'family': 'omlapp-worker'},
  {'name': 'background_dialer_tasks', 'unit': 'background_dialer_tasks.service', 'family': 'omlapp-worker'},
  {'name': 'call_logger', 'unit': 'call_logger.service', 'family': 'omlapp-worker'},
  {'name': 'daily_redis_cleanup', 'unit': 'daily_redis_cleanup.service', 'family': 'omlapp-worker'},
  {'name': 'dashboard_agent_scheduler', 'unit': 'dashboard_agent_scheduler.service', 'family': 'omlapp-worker'},
  {'name': 'supervision_agentes_scheduler', 'unit': 'supervision_agentes_scheduler.service', 'family': 'omlapp-worker'},
  {'name': 'supervision_events_listener', 'unit': 'supervision_events_listener.service', 'family': 'omlapp-worker'},
  {'name': 'presence_heartbeat_scheduler', 'unit': 'presence_heartbeat_scheduler.service', 'family': 'omlapp-worker'},
  {'name': 'kamailio_pstn', 'unit': 'kamailio_pstn.container', 'family': 'telephony'},
  {'name': 'kamailio_webrtc', 'unit': 'kamailio_webrtc.container', 'family': 'telephony'},
  {'name': 'rtpengine', 'unit': 'rtpengine.container', 'family': 'telephony'},
  {'name': 'websockets', 'unit': 'websockets.container', 'family': 'communication'},
  {'name': 'callrec_compressor', 'unit': 'callrec_compressor.container', 'family': 'telephony'},
  {'name': 'callrec_transcriber', 'unit': 'callrec_transcriber.container', 'family': 'telephony'},
  {'name': 'postgresql', 'unit': 'postgresql.container', 'family': 'infrastructure'},
  {'name': 'redis', 'unit': 'redis.container', 'family': 'infrastructure'},
  {'name': 'omnileads', 'unit': 'omnileads.container', 'family': 'omnileads'},
  {'name': 'daphne', 'unit': 'daphne.service', 'family': 'communication'},
  {'name': 'gearman', 'unit': 'gearman.service', 'family': 'processing'},
] %}
{% for service in aio_services_full %}
  - job_name: {{ service.name }}
    journal:
      json: false
      max_age: 6h
      path: /var/log/journal
      matches: "_SYSTEMD_UNIT={{ service.unit }}"
      labels:
        job: {{ service.name }}
        tenant: {{ tenant_id | default('unknown') }}
        host: {{ inventory_hostname }}
        node_type: {{ promtail_compute_node_type }}
        service_family: {{ service.family }}
    relabel_configs:
      - source_labels: ['__journal__systemd_unit']
        target_label: 'unit'
{% endfor %}
{{ dialer_all_services_block() }}
{% endif %}
{% if inventory_hostname in groups.get('data_statefull', []) %}
# ============================================
# data_statefull: PostgreSQL + MinIO
# ============================================
{% set statefull_services = [
  {'name': 'postgresql', 'unit': 'postgresql.container', 'family': 'infrastructure'},
  {'name': 'minio', 'unit': 'minio.service', 'family': 'infrastructure'},
] %}
{% for service in statefull_services %}
  - job_name: {{ service.name }}
    journal:
      json: false
      max_age: 6h
      path: /var/log/journal
      matches: "_SYSTEMD_UNIT={{ service.unit }}"
      labels:
        job: {{ service.name }}
        tenant: {{ tenant_id | default('unknown') }}
        host: {{ inventory_hostname }}
        node_type: data_statefull
        service_family: {{ service.family }}
    relabel_configs:
      - source_labels: ['__journal__systemd_unit']
        target_label: 'unit'
{% endfor %}
{% endif %}
{% if inventory_hostname in groups.get('data_stateless', []) %}
# ============================================
# data_stateless: Redis + Gearman
# ============================================
{% set stateless_services = [
  {'name': 'redis', 'unit': 'redis.container', 'family': 'infrastructure'},
  {'name': 'gearman', 'unit': 'gearman.service', 'family': 'processing'},
] %}
{% for service in stateless_services %}
  - job_name: {{ service.name }}
    journal:
      json: false
      max_age: 6h
      path: /var/log/journal
      matches: "_SYSTEMD_UNIT={{ service.unit }}"
      labels:
        job: {{ service.name }}
        tenant: {{ tenant_id | default('unknown') }}
        host: {{ inventory_hostname }}
        node_type: data_stateless
        service_family: {{ service.family }}
    relabel_configs:
      - source_labels: ['__journal__systemd_unit']
        target_label: 'unit'
{% endfor %}
{% endif %}
{% if inventory_hostname in groups.get('edge', []) %}
# ============================================
# edge: telephony_edge pod
# ============================================
{% set edge_services = [
  {'name': 'haproxy', 'unit': 'haproxy.container', 'family': 'edge'},
  {'name': 'kamailio_pstn', 'unit': 'kamailio_pstn.container', 'family': 'telephony'},
  {'name': 'kamailio_webrtc', 'unit': 'kamailio_webrtc.container', 'family': 'telephony'},
  {'name': 'rtpengine', 'unit': 'rtpengine.container', 'family': 'telephony'},
] %}
{% for service in edge_services %}
  - job_name: {{ service.name }}
    journal:
      json: false
      max_age: 6h
      path: /var/log/journal
      matches: "_SYSTEMD_UNIT={{ service.unit }}"
      labels:
        job: {{ service.name }}
        tenant: {{ tenant_id | default('unknown') }}
        host: {{ inventory_hostname }}
        node_type: edge
        service_family: {{ service.family }}
    relabel_configs:
      - source_labels: ['__journal__systemd_unit']
        target_label: 'unit'
{% endfor %}
{% endif %}
{% if inventory_hostname in groups.get('acd', []) %}
# ============================================
# acd pod
# ============================================
{% set acd_services = [
  {'name': 'acd-app', 'unit': 'acd-app.container', 'family': 'acd'},
  {'name': 'acd-conf', 'unit': 'acd-conf.container', 'family': 'acd'},
  {'name': 'acd-server', 'unit': 'acd-server.container', 'family': 'acd'},
  {'name': 'acd-fastagi', 'unit': 'acd-fastagi.container', 'family': 'acd'},
] %}
{% for service in acd_services %}
  - job_name: {{ service.name }}
    journal:
      json: false
      max_age: 6h
      path: /var/log/journal
      matches: "_SYSTEMD_UNIT={{ service.unit }}"
      labels:
        job: {{ service.name }}
        tenant: {{ tenant_id | default('unknown') }}
        host: {{ inventory_hostname }}
        node_type: acd
        service_family: {{ service.family }}
    relabel_configs:
      - source_labels: ['__journal__systemd_unit']
        target_label: 'unit'
{% endfor %}
{% endif %}
{% if inventory_hostname in groups.get('omlapp_web', []) %}
# ============================================
# omlapp_web pod (sin AIO)
# ============================================
{% set web_services = [
  {'name': 'websockets', 'unit': 'websockets.container', 'family': 'communication'},
  {'name': 'omnileads', 'unit': 'omnileads.container', 'family': 'omnileads'},
  {'name': 'daphne', 'unit': 'daphne.service', 'family': 'communication'},
] %}
{% for service in web_services %}
  - job_name: {{ service.name }}
    journal:
      json: false
      max_age: 6h
      path: /var/log/journal
      matches: "_SYSTEMD_UNIT={{ service.unit }}"
      labels:
        job: {{ service.name }}
        tenant: {{ tenant_id | default('unknown') }}
        host: {{ inventory_hostname }}
        node_type: web
        service_family: {{ service.family }}
    relabel_configs:
      - source_labels: ['__journal__systemd_unit']
        target_label: 'unit'
{% endfor %}
{% endif %}
{% if inventory_hostname in groups.get('omlapp_workers', []) %}
# ============================================
# omlapp_workers pod
# ============================================
{% set worker_services = [
  {'name': 'background_callrec_tasks', 'unit': 'background_callrec_tasks.service', 'family': 'omlapp-worker'},
  {'name': 'background_dialer_tasks', 'unit': 'background_dialer_tasks.service', 'family': 'omlapp-worker'},
  {'name': 'call_logger', 'unit': 'call_logger.service', 'family': 'omlapp-worker'},
  {'name': 'daily_redis_cleanup', 'unit': 'daily_redis_cleanup.service', 'family': 'omlapp-worker'},
  {'name': 'dashboard_agent_scheduler', 'unit': 'dashboard_agent_scheduler.service', 'family': 'omlapp-worker'},
  {'name': 'supervision_agentes_scheduler', 'unit': 'supervision_agentes_scheduler.service', 'family': 'omlapp-worker'},
  {'name': 'supervision_events_listener', 'unit': 'supervision_events_listener.service', 'family': 'omlapp-worker'},
  {'name': 'presence_heartbeat_scheduler', 'unit': 'presence_heartbeat_scheduler.service', 'family': 'omlapp-worker'},
] %}
{% for service in worker_services %}
  - job_name: {{ service.name }}
    journal:
      json: false
      max_age: 6h
      path: /var/log/journal
      matches: "_SYSTEMD_UNIT={{ service.unit }}"
      labels:
        job: {{ service.name }}
        tenant: {{ tenant_id | default('unknown') }}
        host: {{ inventory_hostname }}
        node_type: workers
        service_family: {{ service.family }}
    relabel_configs:
      - source_labels: ['__journal__systemd_unit']
        target_label: 'unit'
{% endfor %}
{% endif %}
{% if inventory_hostname in groups.get('callrec_processor', []) %}
# ============================================
# callrec_processor pod
# ============================================
{% set callrec_services = [
  {'name': 'callrec_compressor', 'unit': 'callrec_compressor.container', 'family': 'telephony'},
  {'name': 'callrec_transcriber', 'unit': 'callrec_transcriber.container', 'family': 'telephony'},
] %}
{% for service in callrec_services %}
  - job_name: {{ service.name }}
    journal:
      json: false
      max_age: 6h
      path: /var/log/journal
      matches: "_SYSTEMD_UNIT={{ service.unit }}"
      labels:
        job: {{ service.name }}
        tenant: {{ tenant_id | default('unknown') }}
        host: {{ inventory_hostname }}
        node_type: callrec
        service_family: {{ service.family }}
    relabel_configs:
      - source_labels: ['__journal__systemd_unit']
        target_label: 'unit'
{% endfor %}
{% endif %}
{% if inventory_hostname in groups.get('dialer_workers', []) and inventory_hostname not in groups.get('omnileads_aio', []) %}
# ============================================
# dialer_workers (cluster; AIO ya incluye dialer arriba)
# ============================================
{{ dialer_all_services_block() }}
{% endif %}