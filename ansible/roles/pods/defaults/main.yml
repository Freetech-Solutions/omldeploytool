---
# Fallback para tags en tasks/main.yml (| default(...)). Mantener alineado con
# group_vars/all/runtime.yml -> pods_role_tags (all group_vars pisa este valor).
pods_role_tags_default:
  - install
  - upgrade
  - update
  - prerequisitos
  - pods
  - data
  - edge
  - nodes
  - web
  - workers
  - voice
  - app
  - postgres
  - minio
  - redis
  - gearman
  - telephony-edge
  - acd
  - websockets
  - omlapp
  - omlapp_workers
  - omlapp-workers
  - interaction_processor
  - dialer
  - nginx
  - haproxy
  - observability
  - addons
  - qa

# Quadlets .pod: membresía en grupos pod del inventario (topology_normalize -> oml_runs_*).
# Cada entrada requiere templates/<nombre>.pod.j2 en este rol.
